<div class="container py-5 px-sm-0">
  <div class="row">
    <div class='mx-auto col-11 col-sm-10 col-lg-8 col-xl-6'>
      <h1>welcome to  <strong>Snackers</strong>  !!</h1>
      <h2>あなたもSnackerになってたくさんのおかしを共有しよう！</h2>
      <h4>続けていくと企業からメッセージが届くかも...</h4>
      <div class="btn-wrapper col-10 mx-auto mt-5">
        <div class="row">
          <%= link_to 'Log in',new_user_session_path, class: "btn btn-info btn-sm btn-block mb-3 sign_in" %>
        </div>
        <div class="row">
          <%= link_to 'Sign Up',new_user_registration_path, class: "btn btn-success btn-sm btn-block sign_up" %>
        </div>
      </div>
    </div>
  </div>
  <h4 class="mt-3 ml-5">参加企業 PICK UP</h4>
  <div class="row mt-3 align-middle text-center">
    <div class="slide fade">
      <%= image_tag @random_company1.get_profile_image(150, 150) %>
      <div><%= @random_company1.name %></div>
    </div>
    <div class="slide fade">
      <%= image_tag @random_company2.get_profile_image(150, 150) %>
       <div><%= @random_company2.name %></div>
    </div>
    <div class="slide fade">
      <%= image_tag @random_company3.get_profile_image(150, 150) %>
       <div><%= @random_company3.name %></div>
    </div>
  </div>
</div>

<script>
  let slideIndex = 0;
  showSlides();

  function showSlides() {
    const slides = document.querySelectorAll(".slide.fade");

    slideIndex++;

    if (slideIndex > slides.length) {
      slideIndex = 1;
    }

    // 3つのスライドを表示
    for (let i = 0; i < slides.length; i++) {
      slides[i].style.opacity = "0";
    }
    for (let i = slideIndex - 1; i < slideIndex + 2; i++) {
      if (i < 0) {
        slides[slides.length - 1].style.opacity = "1";
      } else if (i >= slides.length) {
        slides[0].style.opacity = "1";
      } else {
        slides[i].style.opacity = "1";
      }
    }

    setTimeout(showSlides, 2000);
  }
</script>




